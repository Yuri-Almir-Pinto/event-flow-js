export enum MouseButtons { Left = 0, Middle = 1, Right = 2 }

export enum MouseEventType { 
    MouseUp = "mouseup", MouseDown = "mousedown", MouseMove = "mousemove", Wheel = "wheel"
}

export enum KeyboardEventType { KeyDown = "keydown", KeyUp = "keyup" }